export enum Order {
    ASC = 'ASC',
    DESC = 'DESC',
    asc = 'asc',
    desc = 'desc',
}

export enum IsFollow {
    FOLLOW = 'follow',
    FOLLOWING = 'following',
}

export enum Status {
    DRAFT = 'draft',
    PUBLIC = 'public',
    UNPUBLIC = 'unpublic',
}
