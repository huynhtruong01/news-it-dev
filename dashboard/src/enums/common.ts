export enum Order {
    ASC = 'ASC',
    DESC = 'DESC',
    asc = 'asc',
    desc = 'desc',
}

export enum Status {
    ALL = 'All',
    DRAFT = 'draft',
    PUBLIC = 'public',
    UNPUBLIC = 'unpublic',
}
