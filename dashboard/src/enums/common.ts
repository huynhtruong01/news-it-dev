export enum Order {
    ASC = 'ASC',
    DESC = 'DESC',
    asc = 'asc',
    desc = 'desc',
}

export enum Status {
    DRAFT = 'draft',
    PUBLIC = 'public',
    UNPUBLIC = 'unpublic',
}

export enum StatusUser {
    ACTIVE = 'active',
    INACTIVE = 'inactive',
}
