export enum RoleSelectValue {
    ADMIN = 1,
    USER = 0,
}

export enum RoleSelectName {
    ADMIN = 'Admin',
    USER = 'User',
}

export enum ActiveSelectValue {
    ACTIVE = 1,
    INACTIVE = 0,
}

export enum ActiveSelectName {
    ACTIVE = 'Active',
    INACTIVE = 'Inactive',
}
